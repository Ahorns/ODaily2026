{
  "name": "odaily-writer",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check public/app.js && node --check functions/api/_middleware.js && node --check functions/api/entry.js && node --check functions/api/projects.js && node --check functions/api/dates.js && node --check functions/lib/entry-format.js && node --check functions/lib/github.js && node --check functions/lib/projects.js",
    "build:functions": "wrangler pages functions build functions --outdir .wrangler/build --project-directory . --minify"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260815.1",
    "wrangler": "^4.123.0"
  }
}
