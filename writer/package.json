{
  "name": "odaily-writer",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check public/app.js && node --check functions/api/entry.js && node --check functions/api/projects.js && node --check functions/api/dates.js"
  }
}
